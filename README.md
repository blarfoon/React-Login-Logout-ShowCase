# React Login Logout Showcase
A little react showcase of a login-logout application.
## Start
To start the application you will need to resolve dependancies with

`npm install`

and then to start the application with

`npm start`
